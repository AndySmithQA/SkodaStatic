{
  "name": "skodastatic-root",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["staticskoda"],
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "build": "cd staticskoda && rm -rf node_modules package-lock.json && npm install --no-audit --no-fund && npm run build && cd .. && rm -rf dist && cp -R staticskoda/dist ./dist",
    "preview": "npm --prefix staticskoda run preview",
    "lint": "npm --prefix staticskoda run lint"
  }
}
